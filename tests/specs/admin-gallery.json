[
  {
    "name": "Manage Gallery shows no access for non-admin",
    "steps": [
      {
        "send": { "callback": "admin:gallery" },
        "expect": [
          {
            "method": "answerCallbackQuery",
            "payload": { "text": "No access" }
          }
        ]
      }
    ]
  }
]
