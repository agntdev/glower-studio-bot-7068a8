[
  {
    "name": "Manage Reviews shows no access for non-admin",
    "steps": [
      {
        "send": { "callback": "admin:reviews" },
        "expect": [
          {
            "method": "answerCallbackQuery",
            "payload": { "text": "No access" }
          }
        ]
      }
    ]
  }
]
