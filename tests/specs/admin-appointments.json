[
  {
    "name": "View Appointments shows no access for non-admin",
    "steps": [
      {
        "send": { "callback": "admin:appointments" },
        "expect": [
          {
            "method": "answerCallbackQuery",
            "payload": { "text": "No access" }
          }
        ]
      }
    ]
  }
]
