[
  {
    "name": "Manage Services shows no access for non-admin",
    "steps": [
      {
        "send": { "callback": "admin:services" },
        "expect": [
          {
            "method": "answerCallbackQuery",
            "payload": { "text": "No access" }
          }
        ]
      }
    ]
  }
]
